# Django core
DJANGO_SECRET_KEY=3csw_e$u@zx^^v3ikehv4k38)_^!+&f0w=50%56iirh#%lmzpp
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# API Key - clients must send this in the X-API-KEY header
API_SECRET_KEY=WF-8FBQFQ0PEuHs9_IrTrLJIp0ZcvYgazloae1FiSod-W8

# Optional: override credential paths (defaults point to credentials/ folder)
# FCM_CRED_JOBS=/absolute/path/to/white-force-jobs-service-account.json
# FCM_CRED_ATTENDANCE=/absolute/path/to/white-force-attendance-service-account.json

# Socket.IO / Redis (production: run with multiple uvicorn workers)
# Set USE_REDIS_SOCKETIO=True once you run more than one uvicorn worker,
# so all workers share the same Socket.IO room state via Redis.
REDIS_URL=redis://127.0.0.1:6379/1
USE_REDIS_SOCKETIO=False
